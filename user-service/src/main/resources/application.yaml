spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.133.1:8848
        namespace: 00a0082e-b0d6-4a45-964a-e865f9e69e49
      config:
        server-addr: 192.168.133.1:8848
        namespace: 00a0082e-b0d6-4a45-964a-e865f9e69e49
        group: DEFAULT_GROUP
  config:
      import: optional:nacos:user-service.yml
server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"              # 暴露所有端点（生产环境按需）
      base-path: /actuator        # 访问前缀
  endpoint:
    health:
      show-details: always        # 显示详细健康信息